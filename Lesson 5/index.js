// type conversion = change the data type of a value to another. Ex. String, Number and Boolean

/*
let age = window.prompt("How old are you?");
age = Number(age); // converts the string into Number
age+=1;

console.log(age, typeof age);
*/

/*
let x = "Pasta";
let y = "Pasta";
let z = "Pasta";
*/

let x = "";
let y = "";
let z = "";

x = Number(x);
y = String(y);
z = Boolean(z); // casting string into a Boolean is a way of finding out if the user input is empty or not.

console.log(x, typeof x);
console.log(y, typeof y);
console.log(z, typeof z);