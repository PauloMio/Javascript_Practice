// Math = built-in object that provides a collection of properties and methods

/*
console.log(Math.PI);   // gives the value of PI which is 3.1415...
console.log(Math.E);
*/

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x);   // rounding of number
// z = Math.floor(x);   // round it down
// z = Math.ceil(x);    // round it up
// z = Math.trunc(x);   // eliminate decimals
// z = Math.pow(x, y);     // raising variable to the power of another variable
// z = Math.sqrt(x);       // Squareroot function
// z = Math.log(x);     // Logarithm

/* Trigonometry */
// z = Math.sin(x);
// z = Math.cos(x);
// z = Math.tan(x);

// z = Math.abs(x);    //absolute value
// z = Math.sign(x);   // sign

// let max = Math.max(x, y, z);   // maximum value
// let min = Math.max(x, y, z);    // minnimun value

console.log(min);

